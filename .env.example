JWT_SECRET=change-me-to-a-long-random-string
# Optional: restrict which origins can call the API (single origin only).
# If you deploy behind Nginx on https://qianmeng.me, set:
# CORS_ORIGIN=https://qianmeng.me
# For local dev:
# CORS_ORIGIN=http://localhost:8787
CORS_ORIGIN=http://localhost:8787

# Gemini (server-side only; NEVER expose to frontend)
GEMINI_API_KEY=change-me

# AI Import (global per-model throttles)
AI_IMPORT_PRO_RPM=10
AI_IMPORT_FLASH_RPM=20
# Do not start two requests closer than this
AI_IMPORT_MIN_START_INTERVAL_MS=1000
# Max in-flight requests per model (defensive concurrency cap)
AI_IMPORT_PRO_MAX_IN_FLIGHT=10
AI_IMPORT_FLASH_MAX_IN_FLIGHT=20
# Upload limits
AI_IMPORT_MAX_FILE_SIZE_BYTES=20971520
# Optional: override uploads dir (default: ./data/ai_uploads)
# AI_UPLOADS_DIR=/var/lib/pharm-sync/ai_uploads
