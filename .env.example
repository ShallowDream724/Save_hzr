JWT_SECRET=change-me-to-a-long-random-string
# Optional: restrict which origins can call the API (single origin only).
# If you deploy behind Nginx on your domain, set:
# CORS_ORIGIN=https://your-domain.example
# For local dev:
# CORS_ORIGIN=http://localhost:8787
CORS_ORIGIN=http://localhost:8787

# Gemini (server-side only; NEVER expose to frontend)
GEMINI_API_KEY=change-me

# Optional: override Gemini API base URL (for gateways / self-hosted proxies).
# Use the API root (NOT /v1beta/models). The server will also accept pasted URLs and trim trailing /v1beta(/models).
# Examples:
# GEMINI_BASE_URL=https://generativelanguage.googleapis.com
# GEMINI_BASE_URL=https://your-gemini-gateway.example
# (SDK-native env name also works): GEMINI_NEXT_GEN_API_BASE_URL=...
# Optional: API version (default v1beta)
# GEMINI_API_VERSION=v1beta
# Optional: request timeout (ms). Image extraction / Pro thinking can take >60s.
# GEMINI_TIMEOUT_MS=240000

# Optional: outbound proxy for Gemini (useful for CN local-dev)
# Node.js fetch does NOT always honor system proxy settings; the server will read these env vars.
# Example (Clash): AI_HTTP_PROXY=http://127.0.0.1:7890
# AI_HTTP_PROXY=http://127.0.0.1:7890

# AI Import (global per-model throttles)
AI_IMPORT_PRO_RPM=10
AI_IMPORT_FLASH_RPM=20
# Do not start two requests closer than this
AI_IMPORT_MIN_START_INTERVAL_MS=1000
# Max in-flight requests per model (defensive concurrency cap)
AI_IMPORT_PRO_MAX_IN_FLIGHT=10
AI_IMPORT_FLASH_MAX_IN_FLIGHT=20
# Upload limits
AI_IMPORT_MAX_FILE_SIZE_BYTES=20971520
# Optional: override uploads dir (default: ./data/ai_uploads)
# AI_UPLOADS_DIR=/var/lib/pharm-sync/ai_uploads
